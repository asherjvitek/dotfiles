#!/usr/bin/env bash

# i do this to every directory i am in
# 1 is neovim
# 2 is where i run commands
#
# could be cool!!
tmux new-window -dn scratch
    # tmux send-keys -t $selected_name "nvim ." c-M
    # tmux new-window -dt $selected_name -n "term"
nvim .
clear
